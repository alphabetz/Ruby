player.rb

@found_treasure new hash
def points
def found_treasure
print treature name and add point of treasure

game.rb
def total_points
print "\n#{total_points} total points from treasures found"

game_spec.rb
it "assigns a treasure for points during a player's turn"
it "computes total points as the sum of all player points"
re-calculate for turn play about w00t and blam

player_spec.rb
it "computes a score as the sum of its health and points" do
it "computes points as the sum of all treasure points" do

store treasure name and it value
snack_carbs = {
  :candy => 15,
}

we will store snack @snack_carbs hash after random it!
and we need snacks for each movie
